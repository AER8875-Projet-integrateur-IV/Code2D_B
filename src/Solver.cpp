/*#include "Solver.h"

// Constructor
Solver::Solver(Mesh &mesh, Input &inputVals){
  // Initialize the first results
  Results Simulation = Results(mesh, inputVals);

  for (int i = 0; i<mesh.nElem; i++){
    if (mesh.eSuel[i]>=mesh.nElem){

    }
  }
}*/
